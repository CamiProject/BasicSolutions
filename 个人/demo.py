# 三道生产级 Python 实操练习题
# （要求：全部代码遵循工程规范，使用虚拟环境、合理异常捕获、模块化拆分，禁止单文件堆砌脚本，生产级健壮性要求。）

题目 1：文件 IO + 模块化工程实战（IO 能力）
需求
实现一个日志分析工具：
读取指定目录下所有 .log 文件；
解析日志，筛选出 ERROR 级别日志；
统计每种错误类型出现次数；
将统计结果写入输出 json 文件；
支持命令行传入日志目录路径、输出文件路径参数。
硬性生产要求
项目拆分：参数解析模块、文件扫描模块、日志解析模块、存储模块；
完善异常处理：文件不存在、权限不足、日志格式异常；
使用类型注解；
跳过空文件、避免重复读取；
增加运行日志记录，不要简单 print；
编写简易调用示例。

import os
import 

题目 2：并发任务调度实战（线程 / 进程 / 协程选型，区分同步 & 异步）
需求
批量请求一批公开测试接口，获取接口返回数据并持久化保存：
给定 100 个接口地址列表；
实现两套方案对比：
方案 A：asyncio + aiohttp 异步协程（IO 密集首选）
方案 B：线程池并发版本
限制并发数量，防止请求超限；
请求超时重试机制，失败任务记录到失败清单。
硬性生产要求
做好并发限流，不能无限创建任务；
区分捕获网络异常、超时异常；
禁止同步阻塞代码污染异步事件循环；
代码可配置并发数、超时时间、重试次数；
最终成功、失败结果落地到本地文件；
简单注释说明：什么场景选用协程、什么场景选用线程池。

题目 3：简易 Agent 工具框架（面向对象 + 上下文管理 + 工程封装，贴近 AI Agent 开发）
需求
实现一个可扩展的 Agent 工具调用框架：
定义基础工具抽象类，支持注册各类工具（示例：文本摘要工具、文件读取工具）；
使用上下文管理器实现 Agent 会话自动初始化、资源自动释放；
支持动态注册 / 卸载工具；
工具执行时自动记录调用日志、执行耗时；
捕获工具运行异常，返回标准化错误信息。
硬性生产要求
使用抽象基类 ABC 约束工具规范；
运用上下文管理器 __enter__ / __exit__；
面向对象设计，高拓展性，新增工具无需修改核心调度代码；
类型提示全覆盖；
增加单元测试示例，测试正常调用、工具异常场景；
项目结构模块化，区分核心调度、工具集、日志组件。
